{
  "name": "tokyo-survey-atlas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173",
    "build": "tsc --noEmit && vite build",
    "start": "node server.mjs",
    "test": "node --experimental-transform-types --test tests/*.test.ts"
  },
  "dependencies": {
    "three": "0.185.1",
    "3d-tiles-renderer": "0.5.2"
  },
  "devDependencies": {
    "vite": "8.2.2",
    "typescript": "7.0.2",
    "@types/three": "0.185.4"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
