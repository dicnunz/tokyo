# Optional native orthophotography and tile compression (Unix/macOS).
# Install into the repository .venv; no global installation is required.
numpy>=1.24,<3
rasterio>=1.3,<2
mapbox-vector-tile>=2,<3
Pillow>=10,<13
shapely>=2,<3
